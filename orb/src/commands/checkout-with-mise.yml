description: >
  Checkout then initialize the mise environment.
parameters:
  checkout-branch:
    type: string
    default: "$CIRCLE_BRANCH"
  checkout-method:
    type: enum
    enum:
      - blobless
      - full
    default: full
steps:
  - checkout:
      method: << parameters.checkout-method >>
  - install-mise
